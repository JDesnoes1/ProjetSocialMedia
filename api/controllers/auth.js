export const register = (req, res) => {
  //Vérifier si l'utilisateur existe
  //Si il existe pas, on créer un nouvel utilisateur
  //Hash le password
};

export const login = (req, res) => {};

export const logout = (req, res) => {};
